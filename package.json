{
  "name": "notes-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend/notes-app && npm run build",
    "install": "cd frontend/notes-app && npm install"
  }
}
